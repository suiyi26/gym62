# 太科新生通关宝典 · 单文件版部署说明

## 你拿到了什么

| 文件 | 必需? | 作用 |
|---|---|---|
| `index.html` | ✅ 必需 | **完整独立整站**（约 342 KB）：全部样式、脚本、三校区手绘 SVG、图标全部内联，无任何外部 CDN/文件依赖 |
| `manifest.json` | 可选 | PWA 应用清单（图标已内联为 data URI）。有它 + sw.js 才能体验完整"添加到主屏幕" |
| `sw.js` | 可选 | Service Worker：开启**离线二次访问**与离线提示 |

> **只上传 `index.html` 一个文件也能完整运行**（双击本地打开、上传到任意静态托管都行）；
> 把三个文件一起上传则获得完整 PWA 离线能力。

## 方式一：GitHub Pages（推荐）

1. 登录 GitHub → 右上角 **＋** → **New repository**，取名如 `tyust-baodian`，选 **Public**，创建。
2. 仓库页点 **uploading an existing file**（或 Add file → Upload files）。
3. 把 `index.html`（建议连同 `manifest.json`、`sw.js`）拖进去，点 **Commit changes**。
4. 仓库 **Settings → Pages**（左侧栏）→ **Build and deployment**：
   - Source 选 **Deploy from a branch**
   - Branch 选 **main**，目录选 **/ (root)**，点 Save
5. 等 1~3 分钟，刷新 Settings → Pages 页，顶部会出现访问地址：
   `https://你的用户名.github.io/tyust-baodian/`
6. 手机浏览器打开该地址即可使用；菜单里选"添加到主屏幕"可像 App 一样全屏安装。

> 更新内容时：重新上传覆盖 `index.html`（sw.js 会自动刷新缓存；若个别设备显示旧版，强刷一次即可）。

## 方式二：双击本地直接用

把 `index.html` 拷到手机/电脑，双击（或用浏览器打开）即可完整浏览，
**无需联网**——所有资料、三校区导览地图、工具全部内置在文件里。

## 方式三：其他静态托管（备选）

同样适用于：Vercel / Netlify（拖拽 `dist` 文件夹上传）、Gitee Pages、
校内服务器、对象存储静态网站等，方法一致：把文件放到网站根目录即可。

## 内置功能清单（与线上版一致）

- 12 大免费攻略板块全文开放，站内搜索 + 目录索引
- 三校区交互导览地图（手绘 SVG 底图、拖拽/缩放/点击楼宇看新生贴士、切校区自动居中、全屏按钮）
- 三校区平面示意图 Tab 切换（原创手绘版，点位严格按参考图）
- 免费工具区：物资勾选清单、绩点测算、转专业自测（本地保存）
- 本地收藏、可保存清单、口碑区、冷知识彩蛋
- 进阶付费参考资料后置展示（文案动态渲染 + 禁复制保护）
- 深色模式、斜向水印、PWA 离线、移动端/电脑端自适应

## 说明

- 远程"服务暂停"开关（status.json）在本单文件版中不存在，工具默认始终可用；
  如以后需要远程下线能力，把原版 `status.json` 一并上传并在其中设 `enabled:false` 即可生效。
- GitHub Pages 首次部署后如个别资源 404，等待 1~2 分钟缓存刷新即可。
